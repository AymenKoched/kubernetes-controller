---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: slackmessages.notifications.aymenkoched
spec:
  group: notifications.aymenkoched
  names:
    kind: SlackMessage
    listKind: SlackMessageList
    plural: slackmessages
    singular: slackmessage
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SlackMessage is the Schema for the slackmessages API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of SlackMessage
            properties:
              deploymentRef:
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                - namespace
                type: object
              text:
                type: string
            required:
            - text
            type: object
          status:
            description: status defines the observed state of SlackMessage
            properties:
              error:
                type: string
              sent:
                type: boolean
              sentAt:
                format: date-time
                type: string
            required:
            - sent
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
